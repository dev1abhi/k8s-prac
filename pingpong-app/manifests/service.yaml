apiVersion: v1
kind: Service
metadata:
  name: pingpong-svc
  namespace: exercises
spec:
  type: ClusterIP  
  selector:
    app: pingpong 
  ports:
    - port: 1235 # Port exposed outside the cluster
      protocol: TCP
      targetPort: 4001 # Container's actual listening port